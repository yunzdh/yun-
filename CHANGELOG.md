# 最近 10 条提交摘要

汇总 `dev` 分支最近十次提交的主要变更（合并描述，非逐条列举）：

- **新增/完善 SUSFS 开关**：为所有内核构建工作流加入 `cancel_susfs` 选项，并修正默认值为布尔 `false`，避免调度时报空字符串导致的条件错误；同步在核心 `gki-kernel.yml` 中按开关跳过 SUSFS 补丁。
- **SUKISU 分支逻辑与说明调整**：完善 SukiSU 构建时的分支选择与说明，确保 stable/dev 分支行为清晰，并在 README 提示高概率 bootloop/稳定性状态。
- **构建参数精简**：移除已不再使用的 `make_release` 输入，减少工作流配置噪音。
- **补丁集成与优化**：在 KernelSU 构建流程中增加 Unicode 零宽绕过修复与内存优化补丁的拉取与应用。
- **文档与格式修正**：README 中增加分支提示、空行与更新记录链接，改善可读性；合并上游改动，保持工作流格式一致。
